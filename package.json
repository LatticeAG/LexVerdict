{
  "name": "lexverdict",
  "version": "0.1.0",
  "private": true,
  "description": "Post-execution verification API — pass or steer in milliseconds",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.109.0"
  }
}
